# Copier en application-local.properties (fichier ignore par Git).
# Ce fichier surcharge OFFER.properties charge depuis le Config Server.

# MySQL (port / mot de passe)
# spring.datasource.url=jdbc:mysql://localhost:3306/gestion_offre_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
# spring.datasource.password=votre_mot_de_passe

# OpenAI (chat assistant)
# ai.api.key=sk-proj-VOTRE_CLE
# ai.api.model=gpt-4o-mini

# Email (ou variables d'environnement MAIL_USERNAME, MAIL_PASSWORD, MAIL_FROM)
# spring.mail.username=
# spring.mail.password=
# app.mail.from=
