# Copier en application-local.properties (non versionne).
# Surcharge SUBCONTRACTING.properties du Config Server.

# spring.datasource.url=jdbc:mysql://localhost:3307/gestion_subcontracting_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
# spring.datasource.password=votre_mot_de_passe

# --- IA (Anthropic / Claude) : matching sous-traitance, coaching, analyse financiere, etc. ---
# Alternative : variable d'environnement ANTHROPIC_API_KEY (voir Config Server SUBCONTRACTING.properties).
# anthropic.api.key=sk-ant-api03-...

# --- Wallet coaching (points) : alertes admin (IDs utilisateurs Keycloak / internes) ---
# coach.wallet.low-balance-threshold=300
# coach.wallet.welcome-bonus-points=2000
# coach.wallet.admin-notify-user-ids=1,2
